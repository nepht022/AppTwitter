adicionar a pagina no view/index
criar uma rota pra nova pagina no route
adicionar o metodo action no index controller
criar um modelo com os atributos e metodos da tabela do BD
fazer as funções de acesso ao bd dentro do modelo
recuperar os valores da pagina via post por uma nova route
instanciar um Container para o novo model e instancia de conexao com o bd
setar os valores dos atributos da instancia do container com os valores recuperados via post
usar um metodo de validação para o salvamento dos dados no bd
usar o metodo do novo modelo para adicionar/atualizar/remover no bd de acordo com os valores recuperados via post

12345 abcdef abc123 



